import 'package:flutter/material.dart';

class AppointmentEdit extends StatelessWidget {
  const AppointmentEdit({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}